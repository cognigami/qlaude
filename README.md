# qlaude

Extract your Claude conversation history to SQLite and browse it from the CLI.

## Setup

1. Run the extractor: `python extract.py <path-to-conversations.json>`
2. Browse with: `python browse_claude.py`

## Requirements

- Python 3.x
